# TradingAgents Dashboard

Read-only Next.js dashboard for the TradingAgents Substanz Edition. It watches `../state/` for `*.md` analysis files emitted by the Claude Code agents and renders persona heatmaps, the bull/bear debate stream, and the trader plan plus risk verdict live via SSE. Run with `cd app && npm install && npm run dev` and open http://localhost:3000.
