---
description: Run a full multi-agent trading analysis on a ticker symbol
argument-hint: TICKER
---

Run the **trading-debate** skill on ticker `$ARGUMENTS`.

If `$ARGUMENTS` is empty, ask the user which ticker they want analyzed.

Begin by invoking the `trading-debate` skill via the Skill tool. Pass the ticker through.
