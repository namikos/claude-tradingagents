---
description: Fast 2-analyst + 3-persona analysis. Skips bull/bear debate. Cost ~$1-2, ETA 2-3 min.
argument-hint: TICKER
---

Run the **trading-debate** skill on ticker `$ARGUMENTS` with `mode=quick` and `style=council`.

If `$ARGUMENTS` is empty, ask the user which ticker they want analyzed.

Invoke the `trading-debate` skill via the Skill tool. Pass the ticker plus the explicit flags `--mode quick --style council`.
